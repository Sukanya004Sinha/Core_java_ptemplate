package LambdaExpression;

public class FunctionalInterface {
    /***
     * if interface contains only one abstract method that is called functional interface example of functional interfaces are runnable, callable, ActionListener, Comparable
     * inside functional interface we can write any number of default , static methods.
     * Need of functional Interface
     * to invoke or call Lambda Expression we need to use functional interface
     * in java 1.8 there is an special annotation to annotate a function interface i.e @FunctionalInterface
     * purpose for writing @FunctionalInterface -> To indicate interface explicitly  as FunctionalInterface*/
     *
}
